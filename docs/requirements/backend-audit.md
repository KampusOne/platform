# Backend correction audit — 21 September 2026

**Historical checkpoint.** The current implementation and configuration are recorded in [September 25 handoff](september-25-handoff.md). The owner’s later Hugging Face-only integration and repost/profile improvements supersede the provider details below.

Status vocabulary follows the master prompt. This records local source/schema and automated-test evidence. No production migration, provider send, payment, source publication or deployment was executed by this workstream.

## Inspected evidence and reproduced issues

Read the entire master TXT, root AGENTS, Worker routes, saved schema snapshot, all affected database migrations and existing integration tests. The university PDF/source audit is provided by the academic workstream, not independently repeated here. Existing app data uses Neon application records and application sessions; custom staff grants are independent of identity-provider metadata.

Reproduced by schema-backed tests: ordinary one-character posts violated the legacy four-character title/summary constraints; MP4 was rejected by the upload signature validator; GET home awarded streak activity; legacy administrator permissions were broad role checks; finance access and university scope needed action-specific enforcement; no academic missing-option persistence/review path; agent receipt/review emails were absent; new trial default was ten rather than twelve months; bank verification was unnecessarily a prerequisite for agent approval; unverified universal GPA grading assumptions existed. These are source/test findings, not live-production reproduction claims.

## Implementations and precise scope

| Requirement IDs | Implementation | Evidence status / limitations |
|---|---|---|
| 5–6, 15–20 | Deny-by-default permission registry, individually provisioned staff permissions/scopes/status, global versus university grants; server-backed scoped workspace queries; existing legacy role mappings preserved. Staff cannot self-provision or alter platform admins. | Verified in test: finance-only grant can access finance/analytics and is denied users, applications, dashboard, operations, audit, staff and another university. Existing-account staff provisioning works; invitation delivery is not implemented (14 partial). |
| 21–24,38–41 | Search includes email; user profile/cover URLs and trusted roles; explicit audited brown-badge status grant/revoke independent of staff, publishing, agent approval and payout powers. | Badge scope/audit/no-privilege-escalation verified in test. Frontend colour is student/admin workstream. |
| 25–32,34–36 | Dedicated generic workspace read APIs for application queues, academic submissions, feed posts, actual store orders, AI request metadata, support cases and audit. Existing legacy management routes now use action permissions and resource scopes. | Real saved schema exercised in test. Generic finance rows are store order activity, not recognized revenue or completed payout evidence. Full domain workflows are not implied by list availability. |
| 7–11 | Strict product event endpoint with documented bounded fields, dedupe and per-user rate limit; no arbitrary properties/private prompts; institution recorded server-side. | Event privacy/dedup verified in test; reporting workspace shows observed events. Full retention/cohort/revenue reporting is incomplete. |
| 42–53 | Separate broadcast workstream mounted below permission middleware; see its audit. | Not claimed from this workstream's tests. |
| 63,70–74 | Ordinary text and supported image/video publishing, MP4 signature verification, ownership/institution checks, idempotent retry with changed-payload conflict, media type response, own archive and content reports. Failed database persistence cleans newly uploaded object. | Text, owned media-only MP4, cross-owner rejection, idempotency and signature tests pass. Provider R2 upload/playback/native-device tests remain unverified. Unattached upload retention/cleanup is not implemented. |
| 79–86 | Separate publishing workstream mounted; explicit audited content.capabilities grants independent of badges, private Q&A inbox, anonymous linkage held privately, consent, publication, delete, immutable poll vote. | Five focused publishing tests passed, including anonymity and cross-university negatives; no production verification. |
| 121–126,130–143,231–237 | AI/streak workstream completed owner-only history, HF/Gemini adapters, atomic reservations, refund policy, truthful failures, explicit check-in and read-only history. | Ten targeted tests passed with mocked providers; see docs/phases/2026-09-21-ai-streak-corrections.md. No real model/image extraction certification. |
| 144–159 | Optional missing department/programme submission enables provisional student profile at any existing catalogue university; exact institution is validated; no invented faculty/department/course is inserted. Admission year and editable expected graduation retained. Admin review links a reviewed existing department with provenance and preserves originals. | Non-first university/provisional profile/source queue verified in test. Existing catalogue still needs approved records; provisionally registering an unlisted university is not silently permitted. Departments/programmes are still distinct-data-model work in progress (150 partial). |
| 32,146,157,159 | Source staging tables and separately tested repeatable importer; globally authorized reviewers may publish or link a selected institution claim after primary-source verification attestation, with atomic audit. Source report flags remain unverified originals. Source rejection is explicit and audited. | Importer six tests passed separately. Publication tests added here cover concurrent review, no auto-publication, safe unknown grading and preserved provenance. No live sources staged or published. |
| 151,157,238 | Unknown grading defaults to `{}` with UNVERIFIED provenance; server only applies reviewed institutional grading. User-entered grade points remain an estimate when no verified scale exists. Source-backed guideline publication includes source page/excerpt/issuer/date/effective date/unit/session and append-only versions; replaced guidelines archive, not overwrite. Student reads respect institution/faculty/department/programme/session/effective date. | Academic publication and applicability tests cover supported scope. No official rule invented from incomplete PDF or made applicable nationally. Dedicated programme catalogue and reviewed duration data remain incomplete. |
| 175–198 | Owner-only server draft/resume with whitelisted fields; extra vendor/tutor/rider questions and owned evidence validation; explicit terms required, draft does not preserve consent. Submission details and receipt outbox durable in one SQL statement; successful submission clears draft. | Draft privacy and existing KYC/application suite verified. Real email delivery not tested. Customer presentation owned by portal workstream. |
| 199–214 | Existing manual review actions retain human authorization and reason; independent identity, guardian evidence and current institution scope retained. Decision email queued within review transaction. Bank changes require finance.review separately. | KYC/guardian/scope regression suite passed. Python automated checking, bulk reviewed selection and evidence-quality classifier remain unimplemented; no detector claimed. |
| 215–224 | Approval now checks identity separately from payout bank eligibility. Existing payouts still enforce full identity+bank check. New payout setup handled by identity workstream. | No money moved; production account resolution and settlement not verified. |
| 228–230 | New eligible vendor trial defaults to twelve months; unique user claim persists across sessions/reinstall. Existing claimed expiries remain unchanged pending explicit policy review. | Twelve-month calculation/repeated-claim rejection tested. Historical entitlement changes intentionally not executed. |
| 239–241 | Explicit validation/permission/provider failure responses, real empty datasets, no mocked inventory or success. | Frontend state/accessibility/device verification belongs to parent and UI workstreams. Entire app cannot yet be certified error-free. |

## Changes and migration ordering

Core changes: `server/src/lib/admin-access.ts`, `server/src/routes/{admin,operations,academic-admin,manage,student,media,applications}.ts`, `server/src/lib/kyc.ts`; guideline read in account route; optional HF bindings. Migration `20260921100000_operations_permissions_academic.sql` is additive and changes only future trial/grading defaults plus post title/summary validation. It does not delete records. Academic publication migration `20260921130000_academic_publication.sql` adds provenance/version fields and a lock-protected publication transaction. AI/publishing migrations 110000 and120000 are separate reviewed workstream additions. Parent integrates notification/broadcast/payout migrations140000/150000/160000.

Deployment must apply reviewed migrations before enabling the new code; new handlers reference added tables/columns. Schema proof must include all correction migrations. Downgrade application first if rolling back; retain added tables/provenance and do not blindly drop student/user records.

## Configuration changes

Current new-only configuration: `RESEND_WEBHOOK_SECRET` for signed email delivery events; optional `EXPO_ACCESS_TOKEN` only when Expo enhanced push security is enabled. Preserve the already supplied `HF_TOKEN`, `HF_CHAT_MODEL`, `HF_VISION_MODEL` and all working storage/auth settings. Do not reintroduce HUGGING_FACE_* or Gemini keys for the current code.

## Residual gaps

Not a complete 241/241 implementation: current source catalogue approval, faculties/programmes/durations normalization and fuller stewardship, provider/model validation, real device notifications, provider settlement/reconciliation, email delivery events, full analytics/retention/reporting, background verification pipeline, large reviewed bulk operations, persona governance, comprehensive storage lifecycle, production observations and cross-platform E2E remain subject to their tracked acceptance gates. Runtime tests use fixture records and mocked providers, never fake production statistics.
