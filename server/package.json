{
  "name": "@kampusone/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@kampusone/contracts": "file:../packages/contracts",
    "@neondatabase/serverless": "^1.1.0",
    "drizzle-orm": "^0.45.2",
    "hash-wasm": "^4.12.0",
    "hono": "4.13.7",
    "jose": "^6.2.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260908.1",
    "typescript": "6.0.3",
    "vitest": "5.0.0",
    "wrangler": "4.130.0"
  }
}
